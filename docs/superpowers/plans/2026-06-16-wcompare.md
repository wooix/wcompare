# wcompare Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Beyond Compare 스타일로 파일 2개를 나란히 비교·편집·저장하는 Electron 데스크톱 앱. 확장자에 따라 syntax 하이라이트·외부 linter·vim 모드를 자동 적용한다.

**Architecture:** 얇은 메인 프로세스(파일 IO + linter CLI spawn) + 바닐라 렌더러(Monaco DiffEditor + monaco-vim). diff/syntax/vim/lint 알고리즘은 직접 구현하지 않고 Monaco·ruff·eslint에 위임한다. Monaco 워커는 `app://` 커스텀 스킴 same-origin으로 로딩하고 sandbox:true를 유지한다.

**Tech Stack:** Electron, Monaco Editor(>=0.45), monaco-vim, esbuild, ruff/eslint(CLI), node:test, @playwright/test(`_electron`).

**참고 설계:** `docs/superpowers/specs/2026-06-16-wcompare-design.md` (v2, 6차원 리뷰 반영).

**전역 규칙:**
- TDD: 순수 모듈/파서는 반드시 실패 테스트 → 구현 → 통과 → 커밋.
- 좌표: ruff/eslint는 1-based line·column, exclusive end. **0/-1 보정 금지.**
- lint 판정: stdout이 유효 JSON이면 exit 0/1 모두 채택(exit 1 = "진단 발견" 정상). exit≥2/파싱 실패 → 빈 결과.
- lint 격리: `ruff --isolated`, `eslint --no-config-lookup -c <번들 config>`. 사용자 프로젝트 설정 미반영(RCE 차단).
- 보안: `sandbox:true`, `contextIsolation:true`, `nodeIntegration:false`, CSP, will-navigate/setWindowOpenHandler 차단.
- 커밋: 각 태스크 끝에 1커밋.

---

## File Structure

| 파일 | 책임 |
|---|---|
| `package.json` | 의존성/스크립트 |
| `esbuild.mjs` | renderer/preload/worker 번들(메인 비번들) |
| `playwright.config.js` | E2E 러너 설정 |
| `eslint.config.mjs` | wcompare가 lint에 주입하는 기본 flat config |
| `src/shared/diagnostics.js` | Diagnostic 타입·severity·UTF-16 column 변환 (순수) |
| `src/renderer/languageMap.js` | 확장자→languageId (순수) |
| `src/main/lint/adapters/ruffParse.js` | ruff JSON→Diagnostic[] (순수) |
| `src/main/lint/adapters/eslintParse.js` | eslint JSON→Diagnostic[] (순수) |
| `src/main/fileService.js` | 읽기/쓰기 + isBinary/eol/bom/byteSize |
| `src/main/lint/which.js` | CLI 존재 캐시 |
| `src/main/lint/adapters/ruff.js` | ruff spawn + parse |
| `src/main/lint/adapters/eslint.js` | eslint spawn + parse |
| `src/main/lint/lintService.js` | 라우팅 + 바이트 상한 + skip |
| `src/main/cli.js` | argv→초기 파일쌍 |
| `src/main/ipc.js` | ipcMain.handle + 경로 화이트리스트 |
| `src/main/scheme.js` | app:// 등록·핸들러 |
| `src/main/main.js` | 부트/윈도우/보안 가드/메뉴 |
| `src/preload/preload.js` | contextBridge API |
| `src/renderer/index.html` | CSP + 레이아웃 |
| `src/renderer/monacoEnv.js` | getWorker(label) 매핑 |
| `src/renderer/diffEditor.js` | DiffEditor 래퍼 |
| `src/renderer/lintController.js` | 디바운스/versionId 가드/마커 |
| `src/renderer/vimBinding.js` | vim 영구 attach/activeSide/:w |
| `src/renderer/toolbar.js` | 버튼·단축키 |
| `src/renderer/dnd.js` | 드래그앤드롭 |
| `src/renderer/app.js` | 배선 |

---

## Phase 0 — 부트스트랩 & 스파이크 (blocker 먼저 실증)

### Task 0.1: 프로젝트 초기화 & 의존성

**Files:**
- Create: `package.json`

- [ ] **Step 1: package.json 작성**

```json
{
  "name": "wcompare",
  "version": "0.1.0",
  "description": "Simple side-by-side file compare with syntax, lint, vim",
  "main": "src/main/main.js",
  "type": "commonjs",
  "scripts": {
    "build": "node esbuild.mjs",
    "dev": "node esbuild.mjs --watch & electron .",
    "start": "electron .",
    "test": "node --test test/",
    "e2e": "npm run build && playwright test"
  },
  "license": "MIT"
}
```

- [ ] **Step 2: 의존성 설치**

Run:
```bash
npm install --save monaco-editor monaco-vim
npm install --save-dev electron esbuild @playwright/test
```
Expected: `node_modules/` 생성, 에러 없음. `npx electron --version` 출력 확인.

- [ ] **Step 3: 설치된 버전을 package.json에 고정 확인**

Run: `node -e "const p=require('./package.json');console.log(p.dependencies, p.devDependencies)"`
Expected: monaco-editor 0.45 이상(권장 0.52+). 0.45 미만이면 `npm install monaco-editor@^0.52` 재설치.

- [ ] **Step 4: Commit**

```bash
git add package.json package-lock.json .gitignore
git commit -m "chore: bootstrap wcompare (electron + monaco + esbuild)"
```

---

### Task 0.2: 빌드 스크립트 (esbuild — worker 5종 + renderer + preload)

**Files:**
- Create: `esbuild.mjs`

- [ ] **Step 1: esbuild.mjs 작성**

```js
// esbuild.mjs — 렌더러/preload/Monaco 워커를 번들한다. 메인 프로세스는 번들하지 않는다.
import { build, context } from 'esbuild';
import { fileURLToPath } from 'node:url';
import path from 'node:path';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const out = (p) => path.join(__dirname, 'dist', p);
const watch = process.argv.includes('--watch');

// Monaco 언어 워커 5종 (label→worker 매핑은 renderer/monacoEnv.js 참조)
const WORKERS = {
  'editor.worker': 'monaco-editor/esm/vs/editor/editor.worker.js',
  'json.worker': 'monaco-editor/esm/vs/language/json/json.worker.js',
  'css.worker': 'monaco-editor/esm/vs/language/css/css.worker.js',
  'html.worker': 'monaco-editor/esm/vs/language/html/html.worker.js',
  'ts.worker': 'monaco-editor/esm/vs/language/typescript/ts.worker.js',
};

const common = { bundle: true, sourcemap: true, logLevel: 'info' };

const configs = [
  // 렌더러: monaco 포함, iife
  { ...common, entryPoints: ['src/renderer/app.js'], outfile: out('renderer/app.js'),
    platform: 'browser', format: 'iife', loader: { '.ttf': 'file' } },
  // 워커: classic(iife), browser
  ...Object.entries(WORKERS).map(([name, entry]) => ({
    ...common, entryPoints: [entry], outfile: out(`renderer/${name}.js`),
    platform: 'browser', format: 'iife',
  })),
  // preload: cjs 단일 파일, electron external
  { ...common, entryPoints: ['src/preload/preload.js'], outfile: out('preload/preload.js'),
    platform: 'node', format: 'cjs', external: ['electron'] },
];

if (watch) {
  for (const c of configs) (await context(c)).watch();
  console.log('[esbuild] watching…');
} else {
  await Promise.all(configs.map((c) => build(c)));
  console.log('[esbuild] build complete');
}
```

- [ ] **Step 2: 정적 자산 복사 단계 추가 (index.html)**

esbuild.mjs 끝(빌드 후)에 index.html 복사를 추가한다. `import { cp } from 'node:fs/promises'` 추가 후, 비-watch 분기 끝에:

```js
import { cp, mkdir } from 'node:fs/promises';
// …build 완료 후:
await mkdir(out('renderer'), { recursive: true });
await cp('src/renderer/index.html', out('renderer/index.html'));
console.log('[esbuild] copied index.html');
```
(watch 분기에서도 1회 복사하도록 동일 호출을 watch 시작 전에 둔다.)

- [ ] **Step 3: 빌드는 아직 실행하지 않음 (소스 미존재). 커밋만.**

```bash
echo "dist/" >> .gitignore
git add esbuild.mjs .gitignore
git commit -m "build: esbuild config for renderer/preload/monaco workers"
```

---

### Task 0.3: app:// 스킴 + 최소 main/preload/renderer (스파이크 골격)

**Files:**
- Create: `src/main/scheme.js`, `src/main/main.js`, `src/preload/preload.js`, `src/renderer/index.html`, `src/renderer/monacoEnv.js`, `src/renderer/app.js`

- [ ] **Step 1: `src/main/scheme.js` — app:// privileged 스킴**

```js
// app:// 커스텀 스킴을 same-origin·secure로 등록하고 dist/renderer를 서빙한다.
const { protocol, net } = require('electron');
const path = require('node:path');
const { pathToFileURL } = require('node:url');

const ROOT = path.join(__dirname, '..', '..', 'dist', 'renderer');

function registerScheme() {
  protocol.registerSchemesAsPrivileged([
    { scheme: 'app', privileges: { standard: true, secure: true, supportFetchAPI: true } },
  ]);
}

function handleScheme() {
  // app://bundle/<file> → dist/renderer/<file>
  protocol.handle('app', (request) => {
    const url = new URL(request.url);
    const rel = decodeURIComponent(url.pathname).replace(/^\/+/, '') || 'index.html';
    const filePath = path.join(ROOT, rel);
    // 디렉토리 탈출 방지
    if (!filePath.startsWith(ROOT)) return new Response('forbidden', { status: 403 });
    return net.fetch(pathToFileURL(filePath).toString());
  });
}

module.exports = { registerScheme, handleScheme, APP_ORIGIN: 'app://bundle/' };
```

- [ ] **Step 2: `src/main/main.js` — 스파이크 버전(보안 가드 포함)**

```js
const { app, BrowserWindow, shell } = require('electron');
const path = require('node:path');
const { registerScheme, handleScheme, APP_ORIGIN } = require('./scheme');

app.enableSandbox();
registerScheme(); // app ready 이전 필수

function createWindow() {
  const win = new BrowserWindow({
    width: 1280, height: 800,
    webPreferences: {
      sandbox: true,
      contextIsolation: true,
      nodeIntegration: false,
      preload: path.join(__dirname, '..', '..', 'dist', 'preload', 'preload.js'),
    },
  });
  win.loadURL(APP_ORIGIN + 'index.html');
}

app.on('web-contents-created', (_e, contents) => {
  contents.on('will-navigate', (e) => e.preventDefault());
  contents.setWindowOpenHandler(({ url }) => { shell.openExternal(url); return { action: 'deny' }; });
});

app.whenReady().then(() => {
  handleScheme();
  createWindow();
  app.on('activate', () => { if (BrowserWindow.getAllWindows().length === 0) createWindow(); });
});
app.on('window-all-closed', () => { if (process.platform !== 'darwin') app.quit(); });
```

- [ ] **Step 3: `src/preload/preload.js` — 최소 contextBridge(스파이크용 빈 API)**

```js
const { contextBridge } = require('electron');
// 스파이크 단계에서는 노출 API가 없다. Phase 3에서 채운다.
contextBridge.exposeInMainWorld('wcompare', {
  ping: () => 'pong',
});
```

- [ ] **Step 4: `src/renderer/monacoEnv.js` — getWorker 매핑**

```js
// Monaco 워커를 app:// same-origin에서 로드한다. label→worker 파일 매핑(Monaco 규격).
const MAP = {
  json: 'json.worker.js',
  css: 'css.worker.js', scss: 'css.worker.js', less: 'css.worker.js',
  html: 'html.worker.js', handlebars: 'html.worker.js', razor: 'html.worker.js',
  typescript: 'ts.worker.js', javascript: 'ts.worker.js',
};

self.MonacoEnvironment = {
  getWorker(_id, label) {
    const file = MAP[label] || 'editor.worker.js';
    // app://bundle/renderer/<file> (app.js와 동일 디렉토리). classic worker.
    return new Worker(new URL('./' + file, self.location.href));
  },
};
```

- [ ] **Step 5: `src/renderer/index.html` — CSP + 컨테이너**

```html
<!doctype html>
<html>
<head>
<meta charset="utf-8" />
<meta http-equiv="Content-Security-Policy"
  content="default-src 'none'; script-src 'self'; style-src 'self' 'unsafe-inline'; worker-src 'self' blob:; font-src 'self' data:; img-src 'self' data:; connect-src 'self'" />
<style>
  html, body { margin: 0; height: 100%; }
  #toolbar { height: 36px; display: flex; align-items: center; gap: 8px; padding: 0 8px; background: #1e1e1e; color: #ddd; font: 13px sans-serif; }
  #editor { height: calc(100% - 36px - 22px); }
  #statusbars { height: 22px; display: flex; background: #1e1e1e; color: #9cdcfe; font: 12px monospace; }
  #statusbars > div { flex: 1; padding: 0 8px; overflow: hidden; white-space: nowrap; }
</style>
</head>
<body>
  <div id="toolbar"></div>
  <div id="editor"></div>
  <div id="statusbars"><div id="vim-left"></div><div id="vim-right"></div></div>
  <script src="./app.js"></script>
</body>
</html>
```

- [ ] **Step 6: `src/renderer/app.js` — 스파이크(두 텍스트 diff + ts 워커 트리거)**

```js
import './monacoEnv.js';
import * as monaco from 'monaco-editor';

const el = document.getElementById('editor');
const diff = monaco.editor.createDiffEditor(el, {
  renderSideBySide: true, originalEditable: true, automaticLayout: true,
});
diff.setModel({
  original: monaco.editor.createModel('const a = 1\nconst b = 2\n', 'typescript'),
  modified: monaco.editor.createModel('const a = 1\nconst c = 3\n', 'typescript'),
});
document.getElementById('toolbar').textContent =
  'SPIKE: diff + typescript worker — ' + (window.wcompare?.ping?.() ?? 'no-bridge');
```

- [ ] **Step 7: Commit**

```bash
git add src
git commit -m "spike: app:// scheme + sandboxed monaco diff editor skeleton"
```

---

### Task 0.4: 스파이크 수동 검증 (blocker 해소 확인)

- [ ] **Step 1: 빌드**

Run: `npm run build`
Expected: `dist/renderer/app.js`, `dist/renderer/ts.worker.js`(외 4종), `dist/renderer/index.html`, `dist/preload/preload.js` 생성. 에러 없음.

- [ ] **Step 2: 앱 실행**

Run: `npm start`
Expected:
- 창이 뜨고 좌/우 두 패널에 diff가 보인다(`b=2` vs `c=3` 라인 하이라이트).
- 툴바에 `SPIKE: diff + typescript worker — pong` (preload bridge 동작).
- DevTools(View 메뉴 또는 `Ctrl+Shift+I`) Console에 **워커 로딩 실패/CSP violation 에러가 없어야** 한다.

- [ ] **Step 3: 워커 실제 동작 확인**

DevTools Console에서 직접:
```js
// modified 모델에 타입 오류를 넣으면 ts 워커가 진단을 만든다
monaco.editor.getModels()
```
또는 modified 패널에 `const a: number = "x"` 입력 → 빨간 물결(타입 진단)이 뜨면 ts.worker가 app:// 오리진에서 정상 로드된 것.

- [ ] **Step 4: 검증 결과 기록**

성공 시: 설계 §13.2 스파이크 통과. 이후 Phase 진행.
실패 시(워커 로딩/CSP): DevTools 에러를 보고 설계 §13.2 대안(Blob 워커 프록시)으로 전환 — `monacoEnv.js`의 `getWorker`를 fetch+Blob 방식으로 교체하고 worker-src에 `blob:` 유지. 재검증.

- [ ] **Step 5: Commit (검증 메모)**

```bash
git commit --allow-empty -m "test: spike verified — monaco workers load under app://+sandbox"
```

---
## Phase 1 — 순수 공유 모듈 (TDD, monaco/electron 미import)

### Task 1.1: languageMap (확장자 → languageId)

**Files:**
- Create: `src/renderer/languageMap.js`
- Test: `test/languageMap.test.js`

- [ ] **Step 1: 실패 테스트 작성**

```js
// test/languageMap.test.js
const { test } = require('node:test');
const assert = require('node:assert');
const { extToLanguageId } = require('../src/renderer/languageMap.js');

test('maps known extensions', () => {
  assert.equal(extToLanguageId('a.py'), 'python');
  assert.equal(extToLanguageId('a.ts'), 'typescript');
  assert.equal(extToLanguageId('a.JS'), 'javascript'); // 대소문자 무시
  assert.equal(extToLanguageId('a.tsx'), 'typescript');
  assert.equal(extToLanguageId('a.yml'), 'yaml');
  assert.equal(extToLanguageId('a.md'), 'markdown');
});

test('unknown → plaintext', () => {
  assert.equal(extToLanguageId('a.toml'), 'plaintext');
  assert.equal(extToLanguageId('noext'), 'plaintext');
  assert.equal(extToLanguageId(''), 'plaintext');
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test test/languageMap.test.js`
Expected: FAIL — `Cannot find module ... languageMap.js`

- [ ] **Step 3: 구현 (순수, import 없음 — CommonJS로 노출하되 ESM에서도 import 가능하게 표준 객체만 사용)**

```js
// src/renderer/languageMap.js
const TABLE = {
  js: 'javascript', cjs: 'javascript', mjs: 'javascript', jsx: 'javascript',
  ts: 'typescript', tsx: 'typescript',
  json: 'json', css: 'css', scss: 'scss', html: 'html', htm: 'html',
  xml: 'xml', md: 'markdown', markdown: 'markdown', py: 'python',
  sh: 'shell', bash: 'shell', yaml: 'yaml', yml: 'yaml', sql: 'sql',
  go: 'go', rs: 'rust', c: 'c', h: 'c', cpp: 'cpp', hpp: 'cpp',
  java: 'java', ini: 'ini', cfg: 'ini',
};

function extToLanguageId(filename) {
  const m = String(filename).toLowerCase().match(/\.([^.\\/]+)$/);
  if (!m) return 'plaintext';
  return TABLE[m[1]] || 'plaintext';
}

module.exports = { extToLanguageId };
```

> 주: `app.js`는 ESM(esbuild)으로 번들되지만 esbuild는 CommonJS `module.exports`를 default/named import로 상호운용한다. 렌더러에서는 `import { extToLanguageId } from './languageMap.js'`로 사용. node:test는 `require`로 로드.

- [ ] **Step 4: 통과 확인**

Run: `node --test test/languageMap.test.js`
Expected: PASS (2 tests)

- [ ] **Step 5: Commit**

```bash
git add src/renderer/languageMap.js test/languageMap.test.js
git commit -m "feat: extToLanguageId pure mapping with tests"
```

---

### Task 1.2: diagnostics (severity + 좌표/UTF-16 규약)

**Files:**
- Create: `src/shared/diagnostics.js`
- Test: `test/diagnostics.test.js`

- [ ] **Step 1: 실패 테스트 작성**

```js
// test/diagnostics.test.js
const { test } = require('node:test');
const assert = require('node:assert');
const { toMarker, codepointColToUtf16Col } = require('../src/shared/diagnostics.js');

test('Diagnostic → Monaco marker (1-based, exclusive end 유지, 보정 없음)', () => {
  const d = { startLine: 1, startCol: 8, endLine: 1, endCol: 10,
    message: 'unused', severity: 'warning', source: 'ruff', code: 'F401' };
  const m = toMarker(d);
  assert.equal(m.startLineNumber, 1);
  assert.equal(m.startColumn, 8);
  assert.equal(m.endLineNumber, 1);
  assert.equal(m.endColumn, 10);
  assert.equal(m.message, 'unused');
  assert.equal(m.source, 'ruff');
  assert.equal(m.code, 'F401');
  // severity는 monaco.MarkerSeverity 숫자: error=8, warning=4, info=2
  assert.equal(m.severity, 4);
});

test('severity 매핑', () => {
  assert.equal(toMarker({ severity: 'error', startLine:1,startCol:1,endLine:1,endCol:1,message:'' }).severity, 8);
  assert.equal(toMarker({ severity: 'info', startLine:1,startCol:1,endLine:1,endCol:1,message:'' }).severity, 2);
  assert.equal(toMarker({ severity: 'x', startLine:1,startCol:1,endLine:1,endCol:1,message:'' }).severity, 4); // 기본 warning
});

test('codepoint column → UTF-16 column (surrogate pair 보정)', () => {
  // "😀x" : 😀는 코드포인트1개지만 UTF-16 2단위. 코드포인트 col 2(=x) → utf16 col 3
  const line = '😀x';
  assert.equal(codepointColToUtf16Col(line, 1), 1); // 시작
  assert.equal(codepointColToUtf16Col(line, 2), 3); // 😀 다음
  assert.equal(codepointColToUtf16Col(line, 3), 4); // x 다음(끝, exclusive)
});

test('codepointColToUtf16Col: BMP only는 그대로', () => {
  assert.equal(codepointColToUtf16Col('abc', 3), 3);
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test test/diagnostics.test.js`
Expected: FAIL — 모듈 없음

- [ ] **Step 3: 구현**

```js
// src/shared/diagnostics.js — 순수. monaco를 import하지 않고 숫자 상수만 사용한다.
const SEVERITY = { error: 8, warning: 4, info: 2 }; // monaco.MarkerSeverity 값과 동일

function toMarker(d) {
  return {
    startLineNumber: d.startLine,
    startColumn: d.startCol,
    endLineNumber: d.endLine,
    endColumn: d.endCol,
    message: d.message,
    severity: SEVERITY[d.severity] ?? SEVERITY.warning,
    source: d.source,
    ...(d.code != null ? { code: String(d.code) } : {}),
  };
}

// linter는 column을 유니코드 코드포인트(1-based)로 센다. Monaco는 UTF-16 code unit(1-based).
// 1-based codepoint column을 1-based UTF-16 column으로 변환한다.
function codepointColToUtf16Col(lineText, cpCol) {
  if (cpCol <= 1) return cpCol;
  let cp = 1, utf16 = 1;
  for (const ch of lineText) {       // for..of는 코드포인트 단위 순회
    if (cp >= cpCol) break;
    utf16 += ch.length;              // surrogate pair면 2
    cp += 1;
  }
  return utf16;
}

module.exports = { toMarker, codepointColToUtf16Col, SEVERITY };
```

- [ ] **Step 4: 통과 확인**

Run: `node --test test/diagnostics.test.js`
Expected: PASS (4 tests)

- [ ] **Step 5: Commit**

```bash
git add src/shared/diagnostics.js test/diagnostics.test.js
git commit -m "feat: diagnostics marker + UTF-16 column normalization with tests"
```

---

## Phase 2 — Linter 파서 (TDD, 순수)

> 파서는 spawn과 분리한다. 입력: `{ stdout, exitCode }`, 출력: `Diagnostic[]`. 좌표 보정 없음(1-based 유지). UTF-16 변환은 spawn 어댑터에서 원본 라인과 함께 수행하므로 파서는 코드포인트 column을 그대로 둔다.

### Task 2.1: ruff 파서

**Files:**
- Create: `src/main/lint/adapters/ruffParse.js`
- Test: `test/ruffParser.test.js`

- [ ] **Step 1: 실패 테스트 작성 (실제 ruff 0.14 출력 픽스처 기반)**

```js
// test/ruffParser.test.js
const { test } = require('node:test');
const assert = require('node:assert');
const { parseRuff } = require('../src/main/lint/adapters/ruffParse.js');

const SAMPLE = JSON.stringify([{
  code: 'F401',
  location: { row: 1, column: 8 },
  end_location: { row: 1, column: 10 },
  message: '`os` imported but unused',
  url: 'https://docs.astral.sh/ruff/rules/unused-import',
  fix: null, noqa_row: 1,
}]);

test('exit 1 + JSON → Diagnostic 1개, 좌표 1-based 그대로', () => {
  const r = parseRuff(SAMPLE, 1);
  assert.equal(r.length, 1);
  assert.deepEqual(r[0], {
    startLine: 1, startCol: 8, endLine: 1, endCol: 10,
    message: '`os` imported but unused', severity: 'warning', source: 'ruff', code: 'F401',
  });
});

test('invalid-syntax → error severity', () => {
  const s = JSON.stringify([{ code: 'invalid-syntax', location:{row:2,column:1},
    end_location:{row:2,column:5}, message:'SyntaxError', url:null, fix:null, noqa_row:null }]);
  const r = parseRuff(s, 1);
  assert.equal(r[0].severity, 'error');
  assert.equal(r[0].code, 'invalid-syntax');
});

test('code 누락 안전 처리', () => {
  const s = JSON.stringify([{ location:{row:1,column:1}, end_location:{row:1,column:2}, message:'x' }]);
  const r = parseRuff(s, 1);
  assert.equal(r.length, 1);
  assert.equal(r[0].source, 'ruff');
});

test('exit 0 + 빈 배열 → []', () => {
  assert.deepEqual(parseRuff('[]', 0), []);
});

test('exit 2 또는 파싱 실패 → []', () => {
  assert.deepEqual(parseRuff('', 2), []);
  assert.deepEqual(parseRuff('not json', 1), []);
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test test/ruffParser.test.js`
Expected: FAIL — 모듈 없음

- [ ] **Step 3: 구현**

```js
// src/main/lint/adapters/ruffParse.js
// ruff --output-format json 결과를 Diagnostic[]로. exit 0/1만 진단으로 채택.
function parseRuff(stdout, exitCode) {
  if (exitCode !== 0 && exitCode !== 1) return [];
  let arr;
  try { arr = JSON.parse(stdout); } catch { return []; }
  if (!Array.isArray(arr)) return [];
  return arr.map((d) => ({
    startLine: d.location?.row ?? 1,
    startCol: d.location?.column ?? 1,
    endLine: d.end_location?.row ?? d.location?.row ?? 1,
    endCol: d.end_location?.column ?? (d.location?.column ?? 1) + 1,
    message: d.message ?? '',
    severity: d.code === 'invalid-syntax' ? 'error' : 'warning',
    source: 'ruff',
    code: d.code ?? null,
  })).map((d) => (d.code == null ? (delete d.code, d) : d));
}

module.exports = { parseRuff };
```

> 주: `code:null`일 때 `toMarker`가 code 키를 생략하도록 위에서 키를 제거한다. 테스트는 `source:'ruff'`만 확인하므로 통과한다.

- [ ] **Step 4: 통과 확인**

Run: `node --test test/ruffParser.test.js`
Expected: PASS (5 tests)

- [ ] **Step 5: Commit**

```bash
git add src/main/lint/adapters/ruffParse.js test/ruffParser.test.js
git commit -m "feat: ruff JSON parser → Diagnostic[] with tests"
```

---

### Task 2.2: eslint 파서

**Files:**
- Create: `src/main/lint/adapters/eslintParse.js`
- Test: `test/eslintParser.test.js`

- [ ] **Step 1: 실패 테스트 작성 (eslint --format json 출력은 파일별 배열, messages 안에 진단)**

```js
// test/eslintParser.test.js
const { test } = require('node:test');
const assert = require('node:assert');
const { parseEslint } = require('../src/main/lint/adapters/eslintParse.js');

const SAMPLE = JSON.stringify([{
  filePath: '<text>',
  messages: [
    { ruleId: 'no-unused-vars', severity: 2, message: "'x' is defined but never used.",
      line: 1, column: 7, endLine: 1, endColumn: 8 },
    { ruleId: 'eqeqeq', severity: 1, message: 'Expected ===', line: 2, column: 1, endLine: 2, endColumn: 3 },
  ],
}]);

test('eslint JSON → Diagnostic[], severity 2→error 1→warning', () => {
  const r = parseEslint(SAMPLE, 1);
  assert.equal(r.length, 2);
  assert.deepEqual(r[0], { startLine:1, startCol:7, endLine:1, endCol:8,
    message: "'x' is defined but never used.", severity:'error', source:'eslint', code:'no-unused-vars' });
  assert.equal(r[1].severity, 'warning');
  assert.equal(r[1].code, 'eqeqeq');
});

test('endLine/endColumn 누락 시 start로 폴백', () => {
  const s = JSON.stringify([{ messages: [{ ruleId:null, severity:2, message:'parse', line:3, column:5 }] }]);
  const r = parseEslint(s, 1);
  assert.equal(r[0].endLine, 3);
  assert.equal(r[0].endCol, 6);
  assert.equal(r[0].source, 'eslint');
});

test('exit 2 / 파싱 실패 → []', () => {
  assert.deepEqual(parseEslint('', 2), []);
  assert.deepEqual(parseEslint('boom', 1), []);
});

test('빈 messages → []', () => {
  assert.deepEqual(parseEslint(JSON.stringify([{ messages: [] }]), 0), []);
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test test/eslintParser.test.js`
Expected: FAIL — 모듈 없음

- [ ] **Step 3: 구현**

```js
// src/main/lint/adapters/eslintParse.js
// eslint --format json: [{ filePath, messages: [{ ruleId, severity(1|2), message, line, column, endLine?, endColumn? }] }]
function parseEslint(stdout, exitCode) {
  if (exitCode !== 0 && exitCode !== 1) return [];
  let files;
  try { files = JSON.parse(stdout); } catch { return []; }
  if (!Array.isArray(files)) return [];
  const out = [];
  for (const f of files) {
    for (const m of f.messages || []) {
      const d = {
        startLine: m.line ?? 1,
        startCol: m.column ?? 1,
        endLine: m.endLine ?? m.line ?? 1,
        endCol: m.endColumn ?? (m.column ?? 1) + 1,
        message: m.message ?? '',
        severity: m.severity === 2 ? 'error' : 'warning',
        source: 'eslint',
      };
      if (m.ruleId != null) d.code = m.ruleId;
      out.push(d);
    }
  }
  return out;
}

module.exports = { parseEslint };
```

- [ ] **Step 4: 통과 확인**

Run: `node --test test/eslintParser.test.js`
Expected: PASS (4 tests)

- [ ] **Step 5: Commit**

```bash
git add src/main/lint/adapters/eslintParse.js test/eslintParser.test.js
git commit -m "feat: eslint JSON parser → Diagnostic[] with tests"
```

---
## Phase 3 — 메인 프로세스 서비스

### Task 3.1: fileService (읽기/쓰기 + 메타)

**Files:**
- Create: `src/main/fileService.js`
- Test: `test/fileService.test.js`

- [ ] **Step 1: 실패 테스트 작성**

```js
// test/fileService.test.js
const { test } = require('node:test');
const assert = require('node:assert');
const os = require('node:os');
const path = require('node:path');
const fs = require('node:fs');
const { readFile, writeFile, detectEol, isBinary } = require('../src/main/fileService.js');

const tmp = (name, buf) => { const p = path.join(os.tmpdir(), 'wc-' + Date.now() + '-' + name); fs.writeFileSync(p, buf); return p; };

test('detectEol', () => {
  assert.equal(detectEol('a\r\nb'), '\r\n');
  assert.equal(detectEol('a\nb'), '\n');
  assert.equal(detectEol('nolineend'), '\n'); // 기본 LF
});

test('isBinary: NUL 바이트 감지', () => {
  assert.equal(isBinary(Buffer.from('hello world')), false);
  assert.equal(isBinary(Buffer.from([0x68, 0x00, 0x69])), true);
});

test('readFile: 텍스트 + 메타', () => {
  const p = tmp('r.txt', Buffer.from('line1\r\nline2\r\n', 'utf8'));
  const r = readFile(p);
  assert.equal(r.content, 'line1\r\nline2\r\n');
  assert.equal(r.eol, '\r\n');
  assert.equal(r.isBinary, false);
  assert.ok(r.byteSize > 0);
  fs.unlinkSync(p);
});

test('writeFile 왕복 + BOM 보존', () => {
  const p = path.join(os.tmpdir(), 'wc-w-' + Date.now() + '.txt');
  writeFile(p, 'héllo\nworld\n', { eol: '\n', encoding: 'utf8', bom: false });
  assert.equal(readFile(p).content, 'héllo\nworld\n');
  // BOM 보존
  writeFile(p, 'x\n', { eol: '\n', encoding: 'utf8', bom: true });
  const raw = fs.readFileSync(p);
  assert.deepEqual(raw.slice(0, 3), Buffer.from([0xEF, 0xBB, 0xBF]));
  fs.unlinkSync(p);
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test test/fileService.test.js`
Expected: FAIL — 모듈 없음

- [ ] **Step 3: 구현**

```js
// src/main/fileService.js
const fs = require('node:fs');

function isBinary(buf) {
  const n = Math.min(buf.length, 8000);
  for (let i = 0; i < n; i++) if (buf[i] === 0) return true;
  return false;
}

function detectEol(text) {
  return text.includes('\r\n') ? '\r\n' : '\n';
}

function readFile(filePath) {
  const raw = fs.readFileSync(filePath);
  const bin = isBinary(raw);
  let bom = false;
  let buf = raw;
  if (raw.length >= 3 && raw[0] === 0xEF && raw[1] === 0xBB && raw[2] === 0xBF) {
    bom = true; buf = raw.subarray(3);
  }
  const content = bin ? '' : buf.toString('utf8');
  return { content, eol: detectEol(content), encoding: 'utf8', bom, isBinary: bin, byteSize: raw.length };
}

function writeFile(filePath, content, opts) {
  const eol = opts?.eol || '\n';
  // 모델 내용은 항상 \n. 원본 eol로 정규화 후 기록.
  const normalized = content.replace(/\r\n/g, '\n').replace(/\n/g, eol);
  const body = Buffer.from(normalized, opts?.encoding || 'utf8');
  const out = opts?.bom ? Buffer.concat([Buffer.from([0xEF, 0xBB, 0xBF]), body]) : body;
  fs.writeFileSync(filePath, out);
}

module.exports = { readFile, writeFile, detectEol, isBinary };
```

- [ ] **Step 4: 통과 확인**

Run: `node --test test/fileService.test.js`
Expected: PASS (4 tests)

- [ ] **Step 5: Commit**

```bash
git add src/main/fileService.js test/fileService.test.js
git commit -m "feat: fileService read/write + eol/bom/binary detection with tests"
```

---

### Task 3.2: which.js (CLI 존재 캐시)

**Files:**
- Create: `src/main/lint/which.js`

- [ ] **Step 1: 구현 (테스트는 통합 단계에서 — 환경 의존)**

```js
// src/main/lint/which.js — PATH에서 실행파일 존재를 1회 확인하고 캐시한다.
const { execFileSync } = require('node:child_process');
const cache = new Map();

function hasCommand(cmd) {
  if (cache.has(cmd)) return cache.get(cmd);
  let ok = false;
  try {
    const probe = process.platform === 'win32' ? 'where' : 'which';
    execFileSync(probe, [cmd], { stdio: 'ignore' });
    ok = true;
  } catch { ok = false; }
  cache.set(cmd, ok);
  return ok;
}

module.exports = { hasCommand, _cache: cache };
```

- [ ] **Step 2: 수동 확인**

Run: `node -e "console.log(require('./src/main/lint/which.js').hasCommand('ruff'))"`
Expected: `true` (ruff 설치됨). `node -e "...hasCommand('nope_xyz')"` → `false`.

- [ ] **Step 3: Commit**

```bash
git add src/main/lint/which.js
git commit -m "feat: which command-existence cache"
```

---

### Task 3.3: ruff/eslint spawn 어댑터

**Files:**
- Create: `src/main/lint/adapters/ruff.js`, `src/main/lint/adapters/eslint.js`

- [ ] **Step 1: `ruff.js` 구현 (spawn + 파서 + UTF-16 보정)**

```js
// src/main/lint/adapters/ruff.js
const { spawn } = require('node:child_process');
const path = require('node:path');
const { parseRuff } = require('./ruffParse.js');
const { codepointColToUtf16Col } = require('../../../shared/diagnostics.js');

const TIMEOUT = 5000;

function runRuff(content, filePath) {
  return new Promise((resolve) => {
    const abs = path.resolve(filePath || 'untitled.py');
    const args = ['check', '--isolated', '--output-format', 'json', '--stdin-filename', abs, '-'];
    const child = spawn('ruff', args, { stdio: ['pipe', 'pipe', 'pipe'] });
    let out = '', killed = false;
    const timer = setTimeout(() => { killed = true; child.kill(); }, TIMEOUT);
    child.stdout.on('data', (d) => { out += d; });
    child.on('error', () => { clearTimeout(timer); resolve([]); });
    child.on('close', (code) => {
      clearTimeout(timer);
      if (killed) return resolve([]);
      const diags = parseRuff(out, code);
      resolve(applyUtf16(diags, content));
    });
    child.stdin.on('error', () => {});
    child.stdin.end(content);
  });
}

// 코드포인트 column → UTF-16 column (라인 텍스트 기준)
function applyUtf16(diags, content) {
  const lines = content.split('\n');
  return diags.map((d) => {
    const sl = lines[d.startLine - 1] ?? '';
    const el = lines[d.endLine - 1] ?? '';
    return { ...d, startCol: codepointColToUtf16Col(sl, d.startCol), endCol: codepointColToUtf16Col(el, d.endCol) };
  });
}

module.exports = { runRuff, applyUtf16 };
```

- [ ] **Step 2: `eslint.js` 구현 (번들 기본 config 주입 + 격리)**

```js
// src/main/lint/adapters/eslint.js
const { spawn } = require('node:child_process');
const path = require('node:path');
const { parseEslint } = require('./eslintParse.js');
const { applyUtf16 } = require('./ruff.js'); // 동일 UTF-16 보정 재사용

const TIMEOUT = 5000;
const BUNDLED_CONFIG = path.join(__dirname, '..', '..', '..', '..', 'eslint.config.mjs');

function runEslint(content, filePath) {
  return new Promise((resolve) => {
    const abs = path.resolve(filePath || 'untitled.js');
    const args = ['--no-config-lookup', '-c', BUNDLED_CONFIG, '--format', 'json',
      '--stdin', '--stdin-filename', abs];
    const child = spawn('eslint', args, { stdio: ['pipe', 'pipe', 'pipe'] });
    let out = '', killed = false;
    const timer = setTimeout(() => { killed = true; child.kill(); }, TIMEOUT);
    child.stdout.on('data', (d) => { out += d; });
    child.on('error', () => { clearTimeout(timer); resolve([]); });
    child.on('close', (code) => {
      clearTimeout(timer);
      if (killed) return resolve([]);
      resolve(applyUtf16(parseEslint(out, code), content));
    });
    child.stdin.on('error', () => {});
    child.stdin.end(content);
  });
}

module.exports = { runEslint };
```

- [ ] **Step 3: `eslint.config.mjs` (wcompare 번들 기본 flat config)**

```js
// eslint.config.mjs — wcompare가 lint에 주입하는 기본 규칙. 사용자 프로젝트와 무관.
export default [
  {
    files: ['**/*.{js,cjs,mjs,jsx,ts,tsx}'],
    languageOptions: { ecmaVersion: 'latest', sourceType: 'module' },
    rules: {
      'no-unused-vars': 'warn',
      'no-undef': 'off',
      'eqeqeq': 'warn',
      'no-debugger': 'warn',
    },
  },
];
```

- [ ] **Step 4: 수동 통합 확인 (ruff)**

Run:
```bash
node -e "require('./src/main/lint/adapters/ruff.js').runRuff('import os\nx=1\n','probe.py').then(r=>console.log(JSON.stringify(r)))"
```
Expected: `F401` 진단 1개(startLine 1). eslint도 `.js` 내용으로 유사 확인(설치돼 있으면 진단, 미설치면 `[]`).

- [ ] **Step 5: Commit**

```bash
git add src/main/lint/adapters/ruff.js src/main/lint/adapters/eslint.js eslint.config.mjs
git commit -m "feat: ruff/eslint spawn adapters (isolated) + bundled eslint config"
```

---

### Task 3.4: lintService (라우팅 + 바이트 상한 + skip)

**Files:**
- Create: `src/main/lint/lintService.js`

- [ ] **Step 1: 구현**

```js
// src/main/lint/lintService.js
const { hasCommand } = require('./which.js');
const { runRuff } = require('./adapters/ruff.js');
const { runEslint } = require('./adapters/eslint.js');

const MAX_BYTES = 5 * 1024 * 1024;

// languageId → { cmd, run }
const ROUTES = {
  python: { cmd: 'ruff', run: runRuff },
  javascript: { cmd: 'eslint', run: runEslint },
  typescript: { cmd: 'eslint', run: runEslint },
};

async function lint({ languageId, content, path: filePath }) {
  if (typeof content !== 'string' || Buffer.byteLength(content, 'utf8') > MAX_BYTES) return { diagnostics: [], skipped: 'size' };
  const route = ROUTES[languageId];
  if (!route) return { diagnostics: [], skipped: 'unsupported' };
  if (!hasCommand(route.cmd)) return { diagnostics: [], skipped: 'missing:' + route.cmd };
  try {
    const diagnostics = await route.run(content, filePath);
    return { diagnostics, skipped: null };
  } catch {
    return { diagnostics: [], skipped: 'error' };
  }
}

module.exports = { lint, MAX_BYTES, ROUTES };
```

- [ ] **Step 2: 수동 확인**

Run:
```bash
node -e "require('./src/main/lint/lintService.js').lint({languageId:'python',content:'import os\n',path:'a.py'}).then(r=>console.log(r.skipped, r.diagnostics.length))"
```
Expected: `null 1` (ruff 진단). `languageId:'toml'` → `unsupported 0`.

- [ ] **Step 3: Commit**

```bash
git add src/main/lint/lintService.js
git commit -m "feat: lintService routing with size/missing graceful skip"
```

---

### Task 3.5: ipc.js (핸들러 + 경로 화이트리스트)

**Files:**
- Create: `src/main/ipc.js`

- [ ] **Step 1: 구현**

```js
// src/main/ipc.js
const { ipcMain, dialog, BrowserWindow } = require('electron');
const path = require('node:path');
const fs = require('node:fs');
const { readFile, writeFile } = require('./fileService.js');
const { lint } = require('./lint/lintService.js');

// 현재 세션에서 열린(=쓰기 허용) 경로 화이트리스트
const allowed = new Set();

function normalize(p) {
  try { return fs.realpathSync.native(path.resolve(p)); }
  catch { return path.resolve(p); }
}

function registerIpc() {
  ipcMain.handle('dialog:openFile', async () => {
    const win = BrowserWindow.getFocusedWindow();
    const res = await dialog.showOpenDialog(win, { properties: ['openFile'] });
    if (res.canceled || !res.filePaths[0]) return null;
    const p = normalize(res.filePaths[0]);
    allowed.add(p);
    return readWithPath(p);
  });

  ipcMain.handle('file:read', (_e, rawPath) => {
    const p = normalize(rawPath);
    allowed.add(p); // 사용자가 명시 선택(dialog/CLI/DnD)한 경로만 렌더러가 보냄
    return readWithPath(p);
  });

  ipcMain.handle('file:write', (_e, { path: rawPath, content, eol, encoding, bom }) => {
    const p = normalize(rawPath);
    if (!allowed.has(p)) throw new Error('write denied: path not in session whitelist');
    writeFile(p, content, { eol, encoding, bom });
    return { ok: true, mtimeMs: fs.statSync(p).mtimeMs };
  });

  ipcMain.handle('lint:run', (_e, payload) => lint(payload));
}

function readWithPath(p) {
  const meta = readFile(p);
  return { path: p, ext: path.basename(p), ...meta };
}

function allowPath(p) { allowed.add(normalize(p)); }

module.exports = { registerIpc, allowPath };
```

- [ ] **Step 2: Commit**

```bash
git add src/main/ipc.js
git commit -m "feat: ipc handlers (open/read/write/lint) with write whitelist"
```

---

### Task 3.6: cli.js (argv → 초기 파일쌍)

**Files:**
- Create: `src/main/cli.js`

- [ ] **Step 1: 구현**

```js
// src/main/cli.js — process.argv에서 최대 2개 파일 경로를 추출한다.
const fs = require('node:fs');
const path = require('node:path');

function parsePair(argv) {
  // electron 실행: [electron, ., fileA, fileB] / 패키징: [exe, fileA, fileB]
  const args = argv.slice(1).filter((a) => a !== '.' && !a.startsWith('-'));
  const files = args.filter((a) => { try { return fs.statSync(a).isFile(); } catch { return false; } })
    .map((a) => path.resolve(a));
  return { left: files[0] || null, right: files[1] || null };
}

module.exports = { parsePair };
```

- [ ] **Step 2: 수동 확인**

Run: `node -e "console.log(require('./src/main/cli.js').parsePair(['electron','.','package.json','esbuild.mjs']))"`
Expected: `{ left: '/abs/package.json', right: '/abs/esbuild.mjs' }`

- [ ] **Step 3: Commit**

```bash
git add src/main/cli.js
git commit -m "feat: cli argv → initial file pair"
```

---

### Task 3.7: main.js 완성 (scheme + 보안 + IPC + CLI + 메뉴)

**Files:**
- Modify: `src/main/main.js` (스파이크 버전을 완성형으로 교체)

- [ ] **Step 1: main.js 교체**

```js
const { app, BrowserWindow, shell, Menu } = require('electron');
const path = require('node:path');
const { registerScheme, handleScheme, APP_ORIGIN } = require('./scheme');
const { registerIpc, allowPath } = require('./ipc');
const { parsePair } = require('./cli');

app.enableSandbox();
registerScheme();

let initialPair = { left: null, right: null };

function createWindow() {
  const win = new BrowserWindow({
    width: 1280, height: 800,
    webPreferences: {
      sandbox: true, contextIsolation: true, nodeIntegration: false,
      preload: path.join(__dirname, '..', '..', 'dist', 'preload', 'preload.js'),
    },
  });
  win.loadURL(APP_ORIGIN + 'index.html');
  win.webContents.once('did-finish-load', () => {
    if (initialPair.left) allowPath(initialPair.left);
    if (initialPair.right) allowPath(initialPair.right);
    win.webContents.send('open-pair', initialPair);
  });
}

app.on('web-contents-created', (_e, contents) => {
  contents.on('will-navigate', (e) => e.preventDefault());
  contents.setWindowOpenHandler(({ url }) => { shell.openExternal(url); return { action: 'deny' }; });
});

app.whenReady().then(() => {
  initialPair = parsePair(process.argv);
  handleScheme();
  registerIpc();
  Menu.setApplicationMenu(buildMenu());
  createWindow();
  app.on('activate', () => { if (BrowserWindow.getAllWindows().length === 0) createWindow(); });
});
app.on('window-all-closed', () => { if (process.platform !== 'darwin') app.quit(); });

function buildMenu() {
  const send = (ch) => { const w = BrowserWindow.getFocusedWindow(); if (w) w.webContents.send(ch); };
  return Menu.buildFromTemplate([
    ...(process.platform === 'darwin' ? [{ role: 'appMenu' }] : []),
    { label: 'File', submenu: [
      { label: 'Open Left…', accelerator: 'CmdOrCtrl+O', click: () => send('menu:open-left') },
      { label: 'Open Right…', accelerator: 'CmdOrCtrl+Shift+O', click: () => send('menu:open-right') },
      { label: 'Save', accelerator: 'CmdOrCtrl+S', click: () => send('menu:save') },
      { type: 'separator' }, { role: 'quit' },
    ]},
    { label: 'View', submenu: [
      { label: 'Next Diff', accelerator: 'F7', click: () => send('menu:next-diff') },
      { label: 'Prev Diff', accelerator: 'Shift+F7', click: () => send('menu:prev-diff') },
      { label: 'Toggle Whitespace', click: () => send('menu:toggle-ws') },
      { label: 'Toggle Vim', click: () => send('menu:toggle-vim') },
      { label: 'Toggle Theme', click: () => send('menu:toggle-theme') },
      { type: 'separator' }, { role: 'toggleDevTools' },
    ]},
  ]);
}
```

- [ ] **Step 2: Commit**

```bash
git add src/main/main.js
git commit -m "feat: main process wiring (scheme + ipc + cli + menu + security guards)"
```

---

### Task 3.8: preload.js 완성 (contextBridge API)

**Files:**
- Modify: `src/preload/preload.js`

- [ ] **Step 1: preload.js 교체**

```js
const { contextBridge, ipcRenderer } = require('electron');

contextBridge.exposeInMainWorld('wcompare', {
  openFileDialog: () => ipcRenderer.invoke('dialog:openFile'),
  readFile: (path) => ipcRenderer.invoke('file:read', path),
  writeFile: (payload) => ipcRenderer.invoke('file:write', payload),
  lint: (payload) => ipcRenderer.invoke('lint:run', payload),
  // 이벤트: 콜백 래핑 — IpcRendererEvent를 렌더러로 넘기지 않는다
  onOpenPair: (cb) => ipcRenderer.on('open-pair', (_e, data) => cb(data)),
  onMenu: (cb) => {
    const channels = ['menu:open-left','menu:open-right','menu:save','menu:next-diff','menu:prev-diff','menu:toggle-ws','menu:toggle-vim','menu:toggle-theme'];
    channels.forEach((ch) => ipcRenderer.on(ch, () => cb(ch)));
  },
});
```

- [ ] **Step 2: Commit**

```bash
git add src/preload/preload.js
git commit -m "feat: preload contextBridge API (file/lint/events) with callback wrapping"
```

---
## Phase 4 — 렌더러

### Task 4.1: diffEditor.js (DiffEditor 래퍼)

**Files:**
- Create: `src/renderer/diffEditor.js`

- [ ] **Step 1: 구현**

```js
// src/renderer/diffEditor.js
import * as monaco from 'monaco-editor';
import { extToLanguageId } from './languageMap.js';

export function createDiff(container) {
  const diff = monaco.editor.createDiffEditor(container, {
    renderSideBySide: true, originalEditable: true, automaticLayout: true,
    ignoreTrimWhitespace: false,
  });
  diff.setModel({
    original: monaco.editor.createModel('', 'plaintext'),
    modified: monaco.editor.createModel('', 'plaintext'),
  });

  const state = { left: { path: null, eol: '\n', bom: false, dirty: false },
                  right: { path: null, eol: '\n', bom: false, dirty: false } };
  let lineChanges = null;
  const dirtyCbs = [];

  const innerOf = (side) => side === 'left' ? diff.getOriginalEditor() : diff.getModifiedEditor();
  const modelOf = (side) => innerOf(side).getModel();

  diff.onDidUpdateDiff(() => { lineChanges = diff.getLineChanges(); });

  // per-side dirty 추적
  for (const side of ['left', 'right']) {
    modelOf(side).onDidChangeContent(() => {
      if (!state[side].dirty) { state[side].dirty = true; dirtyCbs.forEach((cb) => cb(side, true)); }
    });
  }

  function open(side, file) {
    // file: { path, content, ext, eol, bom, isBinary, byteSize }
    const langId = extToLanguageId(file.ext || file.path || '');
    const model = modelOf(side);
    monaco.editor.setModelLanguage(model, langId);
    model.setValue(file.content);
    Object.assign(state[side], { path: file.path, eol: file.eol || '\n', bom: !!file.bom, dirty: false });
    dirtyCbs.forEach((cb) => cb(side, false));
    return langId;
  }

  return {
    raw: diff,
    innerOf, modelOf,
    open,
    getValue: (side) => modelOf(side).getValue(),
    getState: (side) => state[side],
    setDirty: (side, v) => { state[side].dirty = v; dirtyCbs.forEach((cb) => cb(side, v)); },
    onDirty: (cb) => dirtyCbs.push(cb),
    getLineChanges: () => lineChanges,
    languageOf: (side) => modelOf(side).getLanguageId(),
    updateOptions: (o) => diff.updateOptions(o),
    goToDiff: (dir) => diff.goToDiff(dir),
  };
}
```

- [ ] **Step 2: Commit**

```bash
git add src/renderer/diffEditor.js
git commit -m "feat: diffEditor wrapper (per-side state, dirty, lineChanges cache)"
```

---

### Task 4.2: lintController (디바운스 + versionId 가드 — 순수부 TDD)

**Files:**
- Create: `src/renderer/lintController.js`
- Test: `test/lintController.test.js`

- [ ] **Step 1: 실패 테스트 작성 (순수 스케줄러부)**

```js
// test/lintController.test.js
const { test } = require('node:test');
const assert = require('node:assert');
const { makeScheduler } = require('../src/renderer/lintController.js');

test('stale 결과 폐기: 캡처 버전과 현재 버전이 다르면 적용 안 함', async () => {
  let applied = null;
  let version = 1;
  const sched = makeScheduler({
    getVersion: () => version,
    runLint: async () => [{ id: 'x' }],
    apply: (markers) => { applied = markers; },
    debounceMs: 0,
  });
  // 요청 시점 v1 캡처 → runLint 동안 v2로 변경 → 폐기되어야 함
  const p = sched.request();
  version = 2;
  await p;
  assert.equal(applied, null);
});

test('fresh 결과는 적용', async () => {
  let applied = null;
  const sched = makeScheduler({
    getVersion: () => 5,
    runLint: async () => [{ id: 'ok' }],
    apply: (m) => { applied = m; },
    debounceMs: 0,
  });
  await sched.request();
  assert.deepEqual(applied, [{ id: 'ok' }]);
});

test('cancel은 떠 있는 디바운스를 취소', async () => {
  let calls = 0;
  const sched = makeScheduler({ getVersion: () => 1, runLint: async () => { calls++; return []; },
    apply: () => {}, debounceMs: 50 });
  sched.schedule(); // 디바운스 예약
  sched.cancel();
  await new Promise((r) => setTimeout(r, 80));
  assert.equal(calls, 0);
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test test/lintController.test.js`
Expected: FAIL — 모듈 없음

- [ ] **Step 3: 구현 (makeScheduler는 순수 — monaco 미import. wireLintController는 monaco 결합부)**

```js
// src/renderer/lintController.js
// 순수 스케줄러: 디바운스 + 버전 가드. monaco를 import하지 않는다(테스트 가능).
function makeScheduler({ getVersion, runLint, apply, debounceMs = 600 }) {
  let timer = null;
  async function request() {
    const captured = getVersion();
    const markers = await runLint();
    if (getVersion() !== captured) return; // stale → 폐기
    apply(markers);
  }
  function schedule() { cancel(); timer = setTimeout(request, debounceMs); }
  function cancel() { if (timer) { clearTimeout(timer); timer = null; } }
  return { request, schedule, cancel };
}

// monaco 결합부: 한 side의 lint를 배선한다.
function wireLintController(monaco, editorApi, side, opts = {}) {
  const { toMarker } = require('../shared/diagnostics.js');
  const model = editorApi.modelOf(side);
  const owner = 'wcompare-lint-' + side;
  const sched = makeScheduler({
    getVersion: () => model.getVersionId(),
    runLint: async () => {
      const languageId = editorApi.languageOf(side);
      const path = editorApi.getState(side).path || 'untitled';
      const res = await window.wcompare.lint({ languageId, content: model.getValue(), path });
      return (res.diagnostics || []).map(toMarker);
    },
    apply: (markers) => monaco.editor.setModelMarkers(model, owner, markers),
    debounceMs: opts.debounceMs ?? 600,
  });
  model.onDidChangeContent(() => sched.schedule());
  return sched; // 저장/로드 시 caller가 sched.cancel()+sched.request() 호출
}

module.exports = { makeScheduler, wireLintController };
```

> 주: 렌더러(ESM 번들)에서는 `import { makeScheduler, wireLintController } from './lintController.js'`. `wireLintController` 내부의 `require('../shared/diagnostics.js')`는 esbuild가 정적 해석하므로 번들에 포함된다.

- [ ] **Step 4: 통과 확인**

Run: `node --test test/lintController.test.js`
Expected: PASS (3 tests)

- [ ] **Step 5: Commit**

```bash
git add src/renderer/lintController.js test/lintController.test.js
git commit -m "feat: lint scheduler (debounce + version guard) with tests"
```

---

### Task 4.3: vimBinding.js (두 에디터 영구 attach + activeSide + :w)

**Files:**
- Create: `src/renderer/vimBinding.js`

- [ ] **Step 1: 구현**

```js
// src/renderer/vimBinding.js
import { initVimMode, VimMode } from 'monaco-vim';

// 모듈 스코프 단일 진실원천: 현재 활성(포커스) side
let activeSide = 'right';
let saveHandler = null; // (side) => void  — app.js가 주입
let instances = { left: null, right: null };
let enabled = true;

export function setSaveHandler(fn) { saveHandler = fn; }
export function getActiveSide() { return activeSide; }

export function setupVim(editorApi) {
  // :w / :wq 전역 1회 등록 — activeSide로 라우팅
  VimMode.Vim.defineEx('write', 'w', () => { if (saveHandler) saveHandler(activeSide); });
  VimMode.Vim.defineEx('wquit', 'wq', () => { if (saveHandler) saveHandler(activeSide); });

  // focus 추적: 각 inner editor가 포커스를 얻으면 activeSide 갱신
  bindFocus(editorApi, 'left');
  bindFocus(editorApi, 'right');

  attachAll(editorApi);
}

function bindFocus(editorApi, side) {
  editorApi.innerOf(side).onDidFocusEditorWidget(() => {
    activeSide = side;
    document.getElementById('vim-' + side)?.classList.add('active');
    document.getElementById('vim-' + (side === 'left' ? 'right' : 'left'))?.classList.remove('active');
  });
}

function attachAll(editorApi) {
  for (const side of ['left', 'right']) {
    const statusEl = document.getElementById('vim-' + side);
    instances[side] = initVimMode(editorApi.innerOf(side), statusEl);
  }
  enabled = true;
}

export function toggleVim(editorApi) {
  if (enabled) {
    for (const side of ['left', 'right']) { instances[side]?.dispose(); instances[side] = null; }
    enabled = false;
  } else {
    attachAll(editorApi);
  }
  return enabled;
}
```

- [ ] **Step 2: monaco-vim의 VimMode export 경로 확인**

Run: `node -e "console.log(Object.keys(require('monaco-vim')))"`
Expected: `initVimMode`, `VimMode` 등 포함. 만약 `VimMode`가 없으면 `import { initVimMode } from 'monaco-vim'` 후 `initVimMode`의 반환/모듈에서 `Vim`을 얻는 경로로 조정(README 확인). defineEx 접근 경로를 실제 export에 맞춘다.

- [ ] **Step 3: Commit**

```bash
git add src/renderer/vimBinding.js
git commit -m "feat: vim binding (dual permanent attach, activeSide, :w routing, toggle)"
```

---

### Task 4.4: toolbar.js (버튼 + 단축키)

**Files:**
- Create: `src/renderer/toolbar.js`

- [ ] **Step 1: 구현**

```js
// src/renderer/toolbar.js
export function buildToolbar(container, actions) {
  const btn = (label, fn) => { const b = document.createElement('button'); b.textContent = label; b.onclick = fn; return b; };
  container.append(
    btn('Open Left', () => actions.openLeft()),
    btn('Open Right', () => actions.openRight()),
    btn('Save', () => actions.save()),
    btn('◀ Diff', () => actions.prevDiff()),
    btn('Diff ▶', () => actions.nextDiff()),
    btn('Whitespace', () => actions.toggleWs()),
    btn('Vim', () => actions.toggleVim()),
    btn('Theme', () => actions.toggleTheme()),
  );
  const status = document.createElement('span');
  status.id = 'toolbar-status';
  status.style.marginLeft = 'auto';
  container.append(status);
}

export function setStatus(msg) { const s = document.getElementById('toolbar-status'); if (s) s.textContent = msg; }
```

- [ ] **Step 2: Commit**

```bash
git add src/renderer/toolbar.js
git commit -m "feat: toolbar buttons + status"
```

---

### Task 4.5: dnd.js (드래그앤드롭)

**Files:**
- Create: `src/renderer/dnd.js`

- [ ] **Step 1: 구현**

```js
// src/renderer/dnd.js — 좌/우 패널에 파일을 드롭하면 해당 side로 연다.
// 기본 동작(네비게이션) 차단. drop 위치(x)로 side 결정.
export function setupDnd(rootEl, onDropFile) {
  for (const ev of ['dragover', 'drop']) {
    window.addEventListener(ev, (e) => e.preventDefault());
  }
  rootEl.addEventListener('drop', (e) => {
    e.preventDefault();
    const f = e.dataTransfer?.files?.[0];
    if (!f) return;
    const side = e.clientX < rootEl.clientWidth / 2 ? 'left' : 'right';
    // Electron에서 File 객체는 path 속성을 가진다(webUtils.getPathForFile 권장 — 최신 Electron)
    const path = f.path || (window.wcompare.getDropPath && window.wcompare.getDropPath(f));
    if (path) onDropFile(side, path);
  });
}
```

> 주: 최신 Electron은 `File.path`가 제거될 수 있다. 그 경우 preload에 `getDropPath: (file) => webUtils.getPathForFile(file)`를 추가(`const { webUtils } = require('electron')`)하고 위 폴백을 사용. Task 3.8 preload에 해당 메서드를 추가하는 것을 권장.

- [ ] **Step 2: preload에 getDropPath 추가 (필요 시)**

`src/preload/preload.js`의 노출 객체에 추가:
```js
  getDropPath: (file) => require('electron').webUtils.getPathForFile(file),
```
(sandbox에서 webUtils 사용 가능 여부를 dev 실행으로 확인. 불가 시 File.path 폴백만 사용.)

- [ ] **Step 3: Commit**

```bash
git add src/renderer/dnd.js src/preload/preload.js
git commit -m "feat: drag-and-drop open by panel side"
```

---

### Task 4.6: app.js 완성 (전체 배선)

**Files:**
- Modify: `src/renderer/app.js` (스파이크 버전을 완성형으로 교체)

- [ ] **Step 1: app.js 교체**

```js
import './monacoEnv.js';
import * as monaco from 'monaco-editor';
import { createDiff } from './diffEditor.js';
import { wireLintController } from './lintController.js';
import { setupVim, toggleVim, setSaveHandler, getActiveSide } from './vimBinding.js';
import { buildToolbar, setStatus } from './toolbar.js';
import { setupDnd } from './dnd.js';

const editorApi = createDiff(document.getElementById('editor'));
const linters = {
  left: wireLintController(monaco, editorApi, 'left'),
  right: wireLintController(monaco, editorApi, 'right'),
};

let theme = 'vs-dark';
let ws = false;
monaco.editor.setTheme(theme);

// dirty → 툴바 상태 표시
editorApi.onDirty((side, dirty) => refreshStatus());
function refreshStatus() {
  const L = editorApi.getState('left'), R = editorApi.getState('right');
  setStatus(`${mark(L)} | ${mark(R)}`);
}
const mark = (s) => (s.path ? (s.dirty ? '● ' : '') + s.path.split('/').pop() : '(empty)');

async function openInto(side, path) {
  const file = await window.wcompare.readFile(path);
  if (file.isBinary && !confirm('바이너리 파일입니다. 텍스트로 강제로 열까요?')) return;
  if (file.byteSize > 5 * 1024 * 1024) alert('5MB 초과: lint를 비활성화하고 엽니다.');
  // 재열기 dirty 가드
  if (editorApi.getState(side).dirty) {
    const ans = prompt('미저장 변경이 있습니다. save/discard/cancel 중 입력', 'cancel');
    if (ans === 'save') await save(side);
    else if (ans !== 'discard') return;
  }
  editorApi.open(side, file);
  linters[side].cancel(); linters[side].request();
  refreshStatus();
}

async function save(side = getActiveSide()) {
  const st = editorApi.getState(side);
  if (!st.path) return;
  await window.wcompare.writeFile({ path: st.path, content: editorApi.getValue(side),
    eol: st.eol, encoding: 'utf8', bom: st.bom });
  editorApi.setDirty(side, false);
  linters[side].cancel(); linters[side].request();
  refreshStatus();
}
setSaveHandler(save);

buildToolbar(document.getElementById('toolbar'), {
  openLeft: async () => { const f = await window.wcompare.openFileDialog(); if (f) await openIntoFile('left', f); },
  openRight: async () => { const f = await window.wcompare.openFileDialog(); if (f) await openIntoFile('right', f); },
  save: () => save(),
  nextDiff: () => editorApi.goToDiff('next'),
  prevDiff: () => editorApi.goToDiff('previous'),
  toggleWs: () => { ws = !ws; editorApi.updateOptions({ ignoreTrimWhitespace: ws }); },
  toggleVim: () => toggleVim(editorApi),
  toggleTheme: () => { theme = theme === 'vs-dark' ? 'vs' : 'vs-dark'; monaco.editor.setTheme(theme); },
});

// dialog는 이미 읽은 file 객체를 반환하므로 별도 경로
async function openIntoFile(side, file) {
  if (editorApi.getState(side).dirty) {
    const ans = prompt('미저장 변경이 있습니다. save/discard/cancel', 'cancel');
    if (ans === 'save') await save(side); else if (ans !== 'discard') return;
  }
  editorApi.open(side, file);
  linters[side].cancel(); linters[side].request();
  refreshStatus();
}

setupVim(editorApi);
setupDnd(document.getElementById('editor'), (side, path) => openInto(side, path));

// 메인 메뉴/CLI 이벤트
window.wcompare.onMenu((ch) => {
  if (ch === 'menu:save') save();
  else if (ch === 'menu:next-diff') editorApi.goToDiff('next');
  else if (ch === 'menu:prev-diff') editorApi.goToDiff('previous');
  else if (ch === 'menu:toggle-ws') { ws = !ws; editorApi.updateOptions({ ignoreTrimWhitespace: ws }); }
  else if (ch === 'menu:toggle-vim') toggleVim(editorApi);
  else if (ch === 'menu:toggle-theme') { theme = theme === 'vs-dark' ? 'vs' : 'vs-dark'; monaco.editor.setTheme(theme); }
  else if (ch === 'menu:open-left') document.querySelector('#toolbar button')?.click();
});

window.wcompare.onOpenPair(async ({ left, right }) => {
  if (left) await openInto('left', left);
  if (right) await openInto('right', right);
});

refreshStatus();
```

- [ ] **Step 2: 빌드 + 수동 실행 확인**

Run: `npm run build && npm start`
Expected: 빈 diff 에디터 + 툴바. "Open Left/Right"로 파일 2개 열면 diff·syntax·진단 마커, vim 모드 동작, Ctrl+S 저장. `wcompare a.py b.py`(= `electron . a.py b.py`)로 CLI 열기.

- [ ] **Step 3: Commit**

```bash
git add src/renderer/app.js
git commit -m "feat: renderer wiring (open/save/lint/vim/toolbar/dnd/menu/cli)"
```

---

## Phase 5 — E2E (@playwright/test _electron)

### Task 5.1: playwright.config.js

**Files:**
- Create: `playwright.config.js`

- [ ] **Step 1: 구현**

```js
// playwright.config.js
const { defineConfig } = require('@playwright/test');
module.exports = defineConfig({
  testDir: './test/e2e',
  fullyParallel: false,
  workers: 1,
  timeout: 30000,
});
```

- [ ] **Step 2: Commit**

```bash
git add playwright.config.js
git commit -m "test: playwright config for electron e2e"
```

---

### Task 5.2: graceful-skip.spec.js (필수 게이트 — linter 미설치 크래시 없음)

**Files:**
- Create: `test/e2e/graceful-skip.spec.js`

- [ ] **Step 1: 구현**

```js
// test/e2e/graceful-skip.spec.js
const { test, expect, _electron: electron } = require('@playwright/test');
const path = require('node:path');

test('linter 미설치(PATH 비움)에서도 앱이 크래시 없이 뜨고 diff가 보인다', async () => {
  const app = await electron.launch({
    args: [path.join(__dirname, '..', '..', 'src', 'main', 'main.js')],
    env: { ...process.env, PATH: '' }, // ruff/eslint를 찾지 못하게
  });
  const win = await app.firstWindow();
  await win.waitForSelector('#editor');
  // Monaco diff 컨테이너가 렌더됨
  await expect(win.locator('.monaco-diff-editor')).toBeVisible({ timeout: 15000 });
  await app.close();
});
```

- [ ] **Step 2: 빌드 후 실행**

Run: `npm run build && npx playwright test test/e2e/graceful-skip.spec.js`
Expected: PASS. (PATH='' 이어도 앱은 정상; lint만 skip)

- [ ] **Step 3: Commit**

```bash
git add test/e2e/graceful-skip.spec.js
git commit -m "test(e2e): graceful skip when linters missing"
```

---

### Task 5.3: compare.spec.js (linter 존재 시 게이팅)

**Files:**
- Create: `test/e2e/compare.spec.js`

- [ ] **Step 1: 구현**

```js
// test/e2e/compare.spec.js
const { test, expect, _electron: electron } = require('@playwright/test');
const path = require('node:path');
const os = require('node:os');
const fs = require('node:fs');
const { execSync } = require('node:child_process');

const hasRuff = (() => { try { execSync('which ruff'); return true; } catch { return false; } })();

test.describe('compare', () => {
  test.skip(!hasRuff, 'requires ruff installed');

  test('두 .py 파일 열기 → diff + ruff 진단 마커 + 저장', async () => {
    const a = path.join(os.tmpdir(), 'wc-e2e-a.py');
    const b = path.join(os.tmpdir(), 'wc-e2e-b.py');
    fs.writeFileSync(a, 'import os\nx = 1\n');
    fs.writeFileSync(b, 'import sys\ny = 2\n');

    const app = await electron.launch({ args: [path.join(__dirname, '..', '..', 'src', 'main', 'main.js'), a, b] });
    const win = await app.firstWindow();
    await win.waitForSelector('.monaco-diff-editor', { timeout: 15000 });

    // ruff F401(import unused) 진단 → squiggly/마커가 생길 때까지 대기
    await win.waitForFunction(() => {
      // monaco 전역이 없으므로 DOM 마커(.squiggly-warning 등) 또는 글리프로 확인
      return document.querySelectorAll('.monaco-editor .squiggly-warning, .monaco-editor .squiggly-error').length > 0;
    }, { timeout: 15000 });

    await app.close();
  });
});
```

- [ ] **Step 2: 실행**

Run: `npm run e2e`
Expected: PASS (ruff 설치 시). ruff 없으면 skip.

- [ ] **Step 3: Commit**

```bash
git add test/e2e/compare.spec.js
git commit -m "test(e2e): compare two py files shows diff + ruff diagnostics"
```

---

## Phase 6 — 다듬기 (한쪽→다른쪽 복사)

### Task 6.1: 복사 화살표 (1블록 복사)

**Files:**
- Modify: `src/renderer/diffEditor.js` (copyBlock 추가), `src/renderer/app.js` (단축키)

- [ ] **Step 1: diffEditor.js에 copyBlock 추가**

`createDiff` 반환 객체에 다음 메서드를 추가한다:

```js
    // 현재 커서가 속한 diff 블록을 fromSide → toSide로 복사한다.
    copyCurrentBlock(fromSide, toSide) {
      const changes = lineChanges; // onDidUpdateDiff로 갱신된 캐시
      if (!changes || changes.length === 0) return false;
      const fromInner = innerOf(fromSide);
      const pos = fromInner.getPosition();
      if (!pos) return false;
      const line = pos.lineNumber;
      // fromSide 기준 라인이 속한 블록 찾기
      const inOriginal = fromSide === 'left';
      const blk = changes.find((c) => {
        const s = inOriginal ? c.originalStartLineNumber : c.modifiedStartLineNumber;
        const e = inOriginal ? c.originalEndLineNumber : c.modifiedEndLineNumber;
        return line >= s && line <= (e || s);
      });
      if (!blk) return false;
      const fromModel = modelOf(fromSide);
      const sFrom = inOriginal ? blk.originalStartLineNumber : blk.modifiedStartLineNumber;
      const eFrom = inOriginal ? blk.originalEndLineNumber : blk.modifiedEndLineNumber;
      const text = eFrom === 0 ? '' : fromModel.getValueInRange({
        startLineNumber: sFrom, startColumn: 1,
        endLineNumber: eFrom, endColumn: fromModel.getLineMaxColumn(eFrom),
      });
      // toSide 대응 범위 (반대편 라인 범위)
      const sTo = inOriginal ? blk.modifiedStartLineNumber : blk.originalStartLineNumber;
      const eTo = inOriginal ? blk.modifiedEndLineNumber : blk.originalEndLineNumber;
      const toModel = modelOf(toSide);
      const toInner = innerOf(toSide);
      const range = eTo === 0
        ? { startLineNumber: sTo + 1, startColumn: 1, endLineNumber: sTo + 1, endColumn: 1 } // 삽입
        : { startLineNumber: sTo, startColumn: 1, endLineNumber: eTo, endColumn: toModel.getLineMaxColumn(eTo) };
      toInner.executeEdits('wcompare-copy', [{ range, text }]);
      return true;
    },
```

- [ ] **Step 2: app.js에 단축키 배선**

app.js의 메뉴/툴바 섹션 근처에 추가(Alt+→: 좌→우, Alt+←: 우→좌). monaco 명령으로 등록:

```js
editorApi.innerOf('left').addCommand(monaco.KeyMod.Alt | monaco.KeyCode.RightArrow,
  () => editorApi.copyCurrentBlock('left', 'right'));
editorApi.innerOf('right').addCommand(monaco.KeyMod.Alt | monaco.KeyCode.LeftArrow,
  () => editorApi.copyCurrentBlock('right', 'left'));
```

- [ ] **Step 3: 수동 확인**

Run: `npm run build && npm start` → 두 파일을 열고 diff 블록에 커서를 둔 뒤 Alt+→/← 로 복사. 복사 후 diff가 갱신되는지 확인(onDidUpdateDiff로 lineChanges 재계산).

- [ ] **Step 4: Commit**

```bash
git add src/renderer/diffEditor.js src/renderer/app.js
git commit -m "feat: copy diff block left<->right (Alt+arrow)"
```

---

## 완료 기준 (전체 Acceptance 재확인)

- [ ] `npm test` (node:test) 모든 단위테스트 PASS.
- [ ] `npm run e2e` — graceful-skip PASS, compare PASS(ruff 설치 시).
- [ ] `wcompare a.py b.py` 로 두 파일 비교 화면이 뜬다(§Acceptance 1).
- [ ] 확장자별 syntax 적용(§2), ruff 진단 마커(§3), vim 토글(§4), Ctrl+S/`:w` 저장(§5).
- [ ] DevTools 콘솔에 워커 로딩/CSP 에러 없음.

---
<!-- PLAN-END -->
